// external import
import { createSlice } from '@reduxjs/toolkit';


const sessionSlice = createSlice({
    name: "Session_info",
    initialState: {
        data: [],
    },
    reducers: {
        
    }
})